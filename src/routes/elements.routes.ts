import express from "express";
// import userController from "../controller/user-controller.js";
// import contactController from "../controller/contact-controller.js";
// import addressController from "../controller/address-controller.js";
// import {authMiddleware} from "../middleware/auth-middleware.js";

const elementRouter = express.Router();
// elementRouter.use(authMiddleware);

// ElementLibrary API
elementRouter.get('/',);
// elementRouter.post('/api/contacts', contactController.create);
// elementRouter.get('/api/contacts/:contactId', contactController.get);
// elementRouter.put('/api/contacts/:contactId', contactController.update);
// elementRouter.delete('/api/contacts/:contactId', contactController.remove);
// elementRouter.get('/api/contacts', contactController.search);