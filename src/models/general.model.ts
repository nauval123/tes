export type positionElement = {
    x : number;
    y : number;
  }
  
  export type dataElement = {
    key: number,
    title: string,
    type_icon: string,
    description: string,
    icon: string, 
  }

  